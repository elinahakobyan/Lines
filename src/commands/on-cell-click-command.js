import { store } from '../models/store';

export function onCellClickCommand(cellUuid) {
  console.warn('aa');
  //   store.game.board.cells.forEach((cell) => {
  //     if (cell.uuid === cellUuid) {
  //       if (cell.isEmpty) {
  //       } else if (!cell.isEmpty && !cell.isActive()) {
  //         cell.activate;
  //       }else if(!cell.isEmpty && cell.isActive()){
  // cell.de
  //       }
  //     }
  //   });
}
